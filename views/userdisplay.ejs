<style>
  * {
    box-sizing: border-box
  }

  button {
    color: white;
    padding: 14px;
    margin: 8px 0;
    border-style: groove;
    cursor: pointer;
    width: 100%;
    opacity: 0.8;
  }

  button:hover {
    opacity: 1;
  }

  .cancelbtn,
  .deletebtn {
    float: left;
    width: 50%;
  }

  .cancelbtn {
    background-color: #ccc;
    color: black;
  }

  .deletebtn {
    background-color: #f44336;
  }

  .container {
    padding: 16px;
    text-align: center;
  }

  /* The Modal (background) */
  .modal {
    display: none;
    position: fixed;
    z-index: 1;
    /* Sit on top */
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    overflow: auto;
    /* Enable scroll if needed */
    background-color: #d9d9d9;
    padding-top: 50px;
  }

  /* Modal Box */
  .modal-content {
    background-color: #fefefe;
    margin: 5% auto 15% auto;
    /* 5% from the top, 15% from the bottom and centered */
    border: 1px solid #888;
    width: 80%;
  }

  /* Style the horizontal ruler */
  hr {
    border: 1px solid #f1f1f1;
    margin-bottom: 25px;
  }

  /* The Modal Close Button (x) */
  .close {
    position: absolute;
    right: 35px;
    top: 15px;
    font-size: 40px;
    font-weight: bold;
    color: black;
  }

  .close:hover,
  .close:focus {
    color: #f44336;
    cursor: pointer;
  }

  /* Clear floats */
  .clearfix::after {
    content: "";
    clear: both;
    display: table;
  }

  /* Change styles for cancel button and delete button on extra small screens */
  @media screen and (max-width: 300px) {

    .cancelbtn,
    .deletebtn {
      width: 100%;
    }
  }
</style>

<% include ./header %>
<div style="overflow-x: auto;">
  <table class="table">
    <thead class="thead-dark">
      <a href="/users/demoregister" class="btn btn-primary">Add User</a>
      <tr>
        <th scope="col">Name</th>
        <th scope="col">Email</th>
        <th scope="col">Action</th>
      </tr>
    </thead>
    <tbody>
      <% users.forEach(function (user) {%>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td>
          <a href="/edituser/<%= user.id%> " class="btn btn-primary" style="margin: 5px;">Edit</a>
          <a href="/delete/<%= user.id%> " class="btn btn-primary" style="margin: 5px;">Delete</a>
          <a href="/adminpermission/<%= user.id%>" class="btn btn-primary" style="margin: 5px" method="POST">Upgrade to Admin</a>
        </td>
      </tr>
      <%}) %>
    </tbody>
  </table>

  <!-- <div id="id01" class="modal">
    <span onclick="document.getElementById('id01').style.display='none'" class="close"
      title="Close Modal">&times;</span>
    <form class="modal-content" action="/action_page.php">
      <div class="container">
        <h1>Delete User's Account</h1>
        <p>Are you sure you want to delete this user?</p>

        <div class="clearfix">
          <a href="/delete/<%= user.id%>">
            <button type="button" class="deletebtn">Delete</button>
          </a>
          <a href="/banuser/<%= user.id%>" method="POST">
            <button type="button" class="cancelbtn">No, just ban it</button>
          </a>
        </div>
      </div>
    </form>
  </div> -->
</div>
<% include ./footer %>

<script>
  // Get the modal
  var modal = document.getElementById('id01');

  // Close the modal by clicking anywhere outside of it
  window.onclick = function (event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
</script>